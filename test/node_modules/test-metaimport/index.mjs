#metamodule


  #keepmacro 'kg'
    post
    TOP
    expand: (arg) ->
      var KG = require 'test-metaimport-relative'
      var code = #quote arg * KG
      code.replaceTag('arg', arg)
      code.replaceTag('KG', arg.newValue(KG))
      code

  
